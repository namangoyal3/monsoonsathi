# Server-only secrets — copy to .env.local (never commit real keys)
# Next.js loads .env.local automatically for next dev / next build

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

OPENWEATHER_API_KEY=
